import { GroupApprovedUsers } from "@/features/groupAdmin/components";

/** Participantes (aprovados) do grupo (PRD-10, PRD10-03). */
export default function GrupoUsuariosAprovadosPage() {
  return <GroupApprovedUsers />;
}
