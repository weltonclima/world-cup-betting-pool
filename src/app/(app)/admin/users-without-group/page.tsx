import { UsersWithoutGroup } from "@/features/superAdmin/components";

/** Usuários sem grupo (super_admin) — atribuição/realocação de grupo. */
export default function UsersWithoutGroupPage() {
  return <UsersWithoutGroup />;
}
