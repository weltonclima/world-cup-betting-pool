/**
 * Placeholder da página de perfil.
 * Conteúdo real será implementado em PRDs futuros.
 */
export default function ProfilePage() {
  return (
    <div className="flex flex-col gap-4">
      <h1 className="text-2xl font-semibold text-foreground">Perfil</h1>
      <p className="text-sm text-muted-foreground">
        Perfil do usuário em construção — conteúdo será adicionado nos próximos PRDs.
      </p>
    </div>
  );
}
