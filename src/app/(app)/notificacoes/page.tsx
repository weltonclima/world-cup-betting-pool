import { NotificationList } from "@/features/notifications/components";

/** Tela 01 — Central de Notificações (PRD-08, PRD08-01). */
export default function NotificacoesPage() {
  return <NotificationList />;
}
