import { PhaseRanking } from "@/features/rankings";

/**
 * Ranking por Fase + Por Grupo (Tela 03) — PRD-05, TASK-09.
 * Server Component fino → componente client. Tema `.ranking-theme` já no shell (TASK-07).
 */
export default function RankingFasePage() {
  return <PhaseRanking />;
}
