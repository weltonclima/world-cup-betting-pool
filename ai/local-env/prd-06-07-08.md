# LOCAL-ENV — Validação local PRD-06 / 07 / 08

> Resultados de validação local executados na sessão. Data: 2026-06-08.

## Comandos e resultados

| Validação | Comando | Resultado |
|---|---|---|
| Tipos | `npx tsc --noEmit` | ✅ **limpo** (0 erros) |
| Lint | `eslint src` | ✅ **0 errors**, 23 warnings (todos **pré-existentes**: `<img>`, exhaustive-deps, unused vars em home/matches/predictions/server — nenhum no código novo) |
| Testes (unidade/integração) | `vitest run` | ✅ **1819/1819** (`success: true`, via reporter JSON) |
| Testes de Rules | `npm run test:rules` (emulador Firestore, Java 17) | ✅ **52/52** |
| Build produção | `next build` | ✅ **exit 0** — todas as rotas novas compilam |

## Rotas novas compiladas (build)

- **PRD-06:** `/profile`, `/profile/configuracoes`, `/profile/editar`, `/profile/estatisticas`,
  `/profile/historico`, `/profile/logout`, `/profile/senha`
- **PRD-07:** `/admin` (redirect), `/admin/dashboard`, `/admin/api-status`, `/admin/logs`,
  `/admin/usuarios/{pendentes,aprovados,bloqueados}`
- **PRD-08:** `/notificacoes`, `/notificacoes/[id]`, `/notificacoes/preferencias`

## Correção de ambiente

- **Build blocker pré-existente corrigido:** `api/predictions/batch/route.ts` exportava
  `BATCH_MAX_SIZE`/`batchInputSchema`/`BatchInput` (proibido pelo Next 15 em arquivo de rota).
  Movidos para `api/predictions/batch/_schema.ts`. Build passou a compilar.

## Limites (não cobertos localmente)

- **UAT runtime com dados reais:** requer Firestore com usuários/admin semeados + sessão autenticada.
  Sugerido validar manualmente: login admin → seção Administração; aprovar usuário → ver log + badge
  de notificação no usuário-alvo; trocar senha; upload de avatar; preferências.
- **Emulador completo (Auth + Firestore)** para fluxos de notificação cross-user end-to-end.

## Veredito: ✅ ambiente local verde (estático). UAT runtime pendente de ambiente semeado.
