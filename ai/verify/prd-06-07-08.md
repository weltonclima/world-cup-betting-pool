# VERIFY (goal-backward) — PRD-06 / 07 / 08

> Verificação goal-backward consolidada das 3 features (`perfil-usuario`,
> `administracao-sistema`, `central-notificacoes`). Confere se o **código entrega o
> objetivo do PRD**, não só se testes passam. Data: 2026-06-08.

## Método
Para cada critério de aceite do PRD, localiza a evidência no código implementado.
Veredito por critério: ✅ atendido · 🟡 atendido com ressalva (placeholder/pendência) · ❌ ausente.

---

## PRD-06 — Perfil do Usuário

| Critério de aceite | Evidência | Veredito |
|---|---|---|
| Usuário visualiza seus dados | `ProfileHub` (nome, @apelido, "desde", status, avatar) ← `useProfile` | ✅ |
| Usuário altera senha | `ChangePasswordForm` → `changePassword` (reauth+update) | ✅ |
| Usuário consulta estatísticas | `PersonalStats` (compõe `useMyRanking`/`useParticipantProfile`) | ✅ |
| Usuário consulta histórico | `PredictionHistory` ← `usePredictionHistory` (predictions+matches+teams) | ✅ |
| Usuário realiza logout | `LogoutConfirm` (signOut + clear cache/localStorage + redirect) | ✅ |
| Funciona mobile+desktop | mobile-first, `.profile-theme`, AppShell responsivo | ✅ |
| Avatar (D-A2) | upload base64 (`fileToCompressedDataUrl`) → `updateProfile`; Rules C27/C28 | ✅ |

**Goal:** ✅ achieved. Pontuação **binária** aplicada (A1): histórico/estatísticas sem "vencedor".

## PRD-07 — Administração

| Critério de aceite | Evidência | Veredito |
|---|---|---|
| Admin acessa dashboard | `/admin/dashboard` (`AdminDashboard`) + seção Admin no Perfil (role-gated) | ✅ |
| Admin aprova/bloqueia usuários | rotas `/admin/usuarios/*` ← `UserStatusList` + `useUpdateUserStatus` | ✅ |
| Admin monitora API | `/admin/api-status` (`ApiStatus`) | 🟡 placeholder honesto (D-A3 — sem telemetria) |
| Logs registrados | `system_logs` (createLog em `useUpdateUserStatus`) + `/admin/logs` | ✅ |
| `/admin/*` só admin | `middleware.ts` (claim `role`) + `AdminGuard` + Rules | ✅ |
| Usuário comum não vê Administração | seção role-gated (`profile.role==="admin"`) | ✅ |
| Funciona mobile+desktop | `.admin-theme`, AppShell | ✅ |

**Goal:** ✅ achieved, com Status API como placeholder declarado (telemetria = pendência D-A3).

## PRD-08 — Central de Notificações

| Critério de aceite | Evidência | Veredito |
|---|---|---|
| Usuário visualiza notificações | `NotificationList` + filtros ← `useNotifications` | ✅ |
| Usuário visualiza badge | `NotificationBell` ← `useUnreadCount` no Header | ✅ |
| Usuário marca como lida | `useMarkAsRead` (ao abrir item + botão no detalhe) | ✅ |
| Usuário altera preferências | `PreferencesForm` (switch auto-save) ← `usePreferences` | ✅ |
| Usuário acessa detalhes | `/notificacoes/[id]` (`NotificationDetail`) | ✅ |
| Funciona mobile+desktop | mobile-first, `.notifications-theme` | ✅ |
| Compatível Spark / sem Cloud Functions | tudo client-side; Rules permitem admin criar p/ terceiro | ✅ |
| Notificações geradas | Sistema (aprovação/bloqueio/reativação) ← `notificationFactory` | 🟡 Ranking/Jogos pendente (D-A5) |

**Goal:** ✅ achieved para o fluxo de Sistema; Ranking/Jogos automáticos = pendência (D-A5).

---

## Veredito consolidado: **goal-achieved**

As 3 features entregam seus objetivos no código. Duas ressalvas são **decisões aceitas** (não falhas):
- Status API = placeholder (D-A3, telemetria não instrumentada).
- Notificações automáticas Ranking/Jogos = pendência (D-A5, sem gatilho server-side no plano Spark).

Limites da verificação: confirma presença/forma no código + build/test verdes. **Não** cobre UAT
runtime com dados reais do Firestore (requer ambiente com usuários/admin semeados — ver `/local-env`).
